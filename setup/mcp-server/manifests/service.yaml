apiVersion: v1
kind: Service
metadata:
  name: stackrox-mcp
  namespace: __MCP_NAMESPACE__
  labels:
    app.kubernetes.io/name: stackrox-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: stackrox-mcp
