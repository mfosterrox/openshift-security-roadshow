site:
  title: OpenShift Security Roadshow
  url: https://www.redhat.com/en/events/openshift-roadshows/security
  start_page: modules::index.adoc

content:
  sources:
    - url: .  # Ensures that the content directory is specified correctly
      start_path: content  # This assumes that the root of your content directory holds the AsciiDoc files

ui:
  bundle:
    url: https://github.com/rhpds/showroom_theme_summit/releases/download/summit-connect-2024/ui-bundle.zip
    snapshot: true

runtime:
  cache_dir: ./.cache/antora  # Keeps your cache in a dedicated folder within the project

output:
  dir: ./www  # Ensures Antora's generated site is saved here
