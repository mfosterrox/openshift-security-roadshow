= {lab_name}

This course offers hands-on experience with Kubernetes-native security on Red Hat^(R)^ OpenShift Container Platform—covering platform foundations, Advanced Cluster Security (RHACS), and Trusted Software Supply Chain (TSSC). Each module ends with a practical *task* so you apply what you learned before moving on.

== How this lab works

* *This page is home* — Credentials and console links live here. Use the home button anytime you get lost.
* *Navigate with the TOC* — Modules are in the left table of contents, or use *Next* at the bottom of each page.
* *Open consoles in a new tab* — Keep this guide open. On a Mac, use *<cmd> click*; on Windows/Linux, use *<ctrl> click* or right-click → "Open in new tab."
* *Use the bastion* — Run `oc`, `roxctl`, and `podman` on the RHEL bastion (SSH details below).
* *Pick a path* — Tracks are self-contained. Start with the path that matches your audience; each module ends with a hands-on scenario.

== Learning paths

* *101 — Foundations* — OpenShift secure-by-default: RBAC, SCC/PSA, network policy, secrets, image provenance, audit logging, compliance basics, and route TLS.
* *201 — Intermediate* — Hardening and shift-left: custom RBAC/SCC, Vault and External Secrets Operator, CI/CD scanning and signing, audit correlation, compliance tailoring, and admission governance.
* *301 — Advanced* — Enterprise zero trust: platform governance, GitOps compliance pipelines, multi-tenant segmentation, runtime detection and forensics, supply chain integrity, and workload identity.
* *RHACS* — Full Advanced Cluster Security lifecycle on realistic workloads: visibility, vulnerabilities, risk, runtime/deploy/build policy, CI/CD gates, compliance, notifications, API, network security, and installation. 
* *TSSC* — Trusted Software Supply Chain: container and git signing with Trusted Artifact Signer, plus SBOM analysis in Trusted Profile Analyzer.

== Environment setup

From the bastion, run the lab environment script. It configures RHACS CLI access (`ROX_CENTRAL_ADDRESS`, `ROX_PASSWORD`, and a generated `ROX_API_TOKEN`), deploys the vulnerable workshop demo applications, and builds/pushes the Quay frontend image used later in the labs.

Cluster-wide RHACS install and baseline settings come from the Showroom image (RHACS demo basic-setup)—this script does not reinstall Central.

[source,bash,subs="attributes",role=execute]
----
git clone https://github.com/mfosterrox/openshift-security-roadshow.git
cd openshift-security-roadshow
bash setup/lab-environment.sh \
  --quay-user {quay_admin_username} \
  --quay-password '{quay_admin_password}'
source ~/.bashrc
----

== All lab components

During the roadshow, you will utilize the following applications to complete all of the lab tasks:

* Red Hat^(R)^ OpenShift Container Platform (RHOCP) console
* Red Hat^(R)^ Advanced Cluster Management (RHACM) console with OpenShift GitOps (ArgoCD)
* Red Hat^(R)^ Advanced Cluster Security (RHACS) console
* Red Hat^(R)^ Quay console
* Red Hat^(R)^ Trusted Artifact Signer (RHTAS)
* Red Hat^(R)^ Enterprise Linux (RHEL) bastion host (The "Showroom" lab environment)

=== Red Hat^(R)^ OpenShift Container Platform (OCP) console information
[cols="1,1"]
|===
|Console URL| {web_console_url}[{web_console_url}^]
|Username| {openshift_admin_user}
|Password| {openshift_admin_password}
|===

Red Hat^(R)^ Advanced Cluster Management (RHACM) and OpenShift GitOps (ArgoCD) are accessed from the same OpenShift console (e.g. *Applications*, *Pipelines*, and GitOps from the left menu or cluster dropdown).

=== Red Hat^(R)^ Advanced Cluster Security (RHACS) console information
[cols="1,1"]
|===
|Console URL| {acs_route}[{acs_route}^]
|Username| {acs_portal_username}
|Password| {acs_portal_password}
|===

=== Red Hat^(R)^ Quay
[cols="1,1"]
|===
|Console URL| {quay_console_url}[{quay_console_url}^]
|Username| {quay_admin_username}
|Password| {quay_admin_password}
|===

=== Red Hat^(R)^ Trusted Profile Analyzer (RHTAP) information
[cols="1,1"]
|===
|Console URL| {tpa_console_url}[{tpa_console_url}^]
|Username| {tpa_username}
|Password| {tpa_password}
|===

=== Red Hat^(R)^ Enterprise Linux (RHEL) bastion host information

Use the bastion to run CLI commands required in the lab (`oc`, `roxctl`, `podman`, `curl`, etc.). SSH to the host below, then follow the steps in each module.

[cols="1,1"]
|===
|Username@host| {bastion_ssh_user_name}@{bastion_public_hostname}
|Password| {bastion_ssh_password}
|===
